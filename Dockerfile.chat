FROM python:3.11-slim

WORKDIR /app

# Install Streamlit
RUN pip install --no-cache-dir streamlit requests

# Copy chat app
COPY chat/app.py ./app.py

EXPOSE 8501

CMD ["streamlit", "run", "app.py", "--server.address=0.0.0.0"]
