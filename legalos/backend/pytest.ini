[pytest]
asyncio_mode = auto
; One event loop for the whole test session: the module-scoped client fixture
; and the app's pooled asyncpg connections must live on the same loop,
; otherwise pool_pre_ping fails with "attached to a different loop".
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
